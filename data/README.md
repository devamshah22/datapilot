# Data directory

This folder holds CSV files used during development and testing.

**Real data files are gitignored** — never commit user data or large datasets to the repo.

To add a development dataset, drop the CSV here and reference it from your code or env.
